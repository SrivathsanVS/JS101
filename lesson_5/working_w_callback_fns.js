// Example 3
// Logs 1 and 3. Returns the samew

// Example 4
let myArr = [[18, 7], [3, 12]].forEach(arr => {
  return arr.map(num => {
    if (num > 5) {
      return console.log(num);
    }
  });
});
